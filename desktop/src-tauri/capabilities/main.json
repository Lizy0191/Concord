{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Local workspace access to the supervised backend and explicit native file selection only.",
  "windows": ["main"],
  "permissions": ["core:default", "allow-connection-info", "allow-import-document"]
}
